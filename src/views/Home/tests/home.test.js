import {wait, waitForElement, fireEvent, render} from '@testing-library/react';
import React from 'react';
import Home from '../HomeContainer';


// Integration Tests using Kent Dodds React Testing Library
describe("Home Tests", () => {
  test("should load the document to the DOM", () => {
    // write tests here
  })
})